openapi: "3.0.3"

info:
  title: ranger
  description: >
    your roadmap lives in your repo. every operation is scoped to a
    configured project by name; every read that paints state carries
    content hashes; every mutation carries them back; every mutation success
    returns a fresh board so the client repaints from disk truth. no auth,
    localhost only.
  version: 1.0.0

servers:
  - url: /api/v1

paths:
  /projects:
    get:
      operationId: getProjects
      summary: >
        the configured project index — each project's name and availability,
        judged by a fresh load at request time, plus the default.
      responses:
        "200":
          description: the project index
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/projectIndex"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/board:
    parameters:
      - $ref: "#/components/parameters/project"
    get:
      operationId: getBoard
      summary: the computed board, rebuilt from a fresh read of the disk
      responses:
        "200":
          description: lanes in lifecycle order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/board"
        "404":
          $ref: "#/components/responses/notFound"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/search:
    parameters:
      - $ref: "#/components/parameters/project"
    get:
      operationId: searchItems
      summary: >
        case-insensitive substring search over item titles and bodies,
        against a fresh read of the disk.
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: matching item filenames
          content:
            application/json:
              schema:
                type: object
                required: [filenames]
                properties:
                  filenames:
                    type: array
                    items:
                      type: string
        "404":
          $ref: "#/components/responses/notFound"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items:
    parameters:
      - $ref: "#/components/parameters/project"
    post:
      operationId: createItem
      summary: capture a new item into inbox
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                body:
                  type: string
      responses:
        "201":
          description: captured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/itemLanded"
        "400":
          $ref: "#/components/responses/validationError"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    get:
      operationId: getItem
      summary: one item's raw content and parsed card
      responses:
        "200":
          description: the item
          content:
            application/json:
              schema:
                type: object
                required: [content, card, hash]
                properties:
                  content:
                    type: string
                  card:
                    $ref: "#/components/schemas/card"
                  hash:
                    type: string
        "404":
          description: no such project or item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/errorResponse"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}/content:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    put:
      operationId: saveContent
      summary: raw save; a state-changing save runs the ranked-transition cleanup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content, expectedHash, expectedOrderVersion]
              properties:
                content:
                  type: string
                expectedHash:
                  type: string
                expectedOrderVersion:
                  type: string
      responses:
        "200":
          $ref: "#/components/responses/freshBoard"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}/state:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    post:
      operationId: transitionItem
      summary: transition, or transition-and-place when position is given
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [state, expectedHash, expectedOrderVersion]
              properties:
                state:
                  $ref: "#/components/schemas/state"
                expectedHash:
                  type: string
                expectedOrderVersion:
                  type: string
                position:
                  type: integer
                  minimum: 0
                  description: >
                    index into the destination lane's ranked list only,
                    never the displayed card sequence.
      responses:
        "200":
          $ref: "#/components/responses/freshBoard"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/order/{lane}:
    parameters:
      - $ref: "#/components/parameters/project"
      - name: lane
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/state"
    put:
      operationId: reorderLane
      summary: rewrite one lane's ranked prefix
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filenames, expectedVersion]
              properties:
                filenames:
                  type: array
                  items:
                    type: string
                  description: >
                    only the resulting ranked prefix, never the whole
                    displayed lane; cards absent from it stay unranked.
                expectedVersion:
                  type: string
      responses:
        "200":
          $ref: "#/components/responses/freshBoard"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}/delete:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    post:
      operationId: deleteItem
      summary: >
        remove an item file — the operator's explicit curation gesture. the
        item's order.yaml entries go in the same gesture.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedHash, expectedOrderVersion]
              properties:
                expectedHash:
                  type: string
                expectedOrderVersion:
                  type: string
      responses:
        "200":
          $ref: "#/components/responses/freshBoard"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}/retitle:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    post:
      operationId: retitleItem
      summary: retitle and rename to the new slug, rank preserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, expectedHash, expectedOrderVersion]
              properties:
                title:
                  type: string
                expectedHash:
                  type: string
                expectedOrderVersion:
                  type: string
      responses:
        "200":
          description: retitled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/itemLanded"
        "400":
          $ref: "#/components/responses/validationError"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

  /projects/{project}/items/{filename}/rename-to-slug:
    parameters:
      - $ref: "#/components/parameters/project"
      - $ref: "#/components/parameters/filename"
    post:
      operationId: renameToSlug
      summary: repair the filename-mismatch flag with one rename
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedHash, expectedOrderVersion]
              properties:
                expectedHash:
                  type: string
                expectedOrderVersion:
                  type: string
      responses:
        "200":
          description: renamed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/itemLanded"
        "400":
          $ref: "#/components/responses/validationError"
        "404":
          $ref: "#/components/responses/notFound"
        "409":
          $ref: "#/components/responses/conflict"
        default:
          $ref: "#/components/responses/serverError"

components:
  parameters:
    project:
      name: project
      in: path
      required: true
      schema:
        type: string

    filename:
      name: filename
      in: path
      required: true
      schema:
        type: string

  responses:
    freshBoard:
      description: the board after the mutation, rebuilt from disk truth
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/board"
    notFound:
      description: no such project
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/errorResponse"
    conflict:
      description: >
        guard refusal. item_conflict and order_conflict mean the view went
        stale — reload. slug_collision is a no-clobber refusal and carries
        structured recovery paths.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/conflict"
    validationError:
      description: the request fails prevalidation; nothing was written
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/errorResponse"
    serverError:
      description: repository-level failure
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/errorResponse"

  schemas:
    state:
      type: string
      enum: [inbox, horizon, researching, building, evaluating]

    flag:
      type: object
      required: [kind, diagnostic]
      properties:
        kind:
          type: string
          enum: [malformed, filename-mismatch]
        diagnostic:
          type: string

    logEntry:
      type: object
      required: [stamp, note]
      properties:
        stamp:
          type: string
        note:
          type: string

    card:
      type: object
      required: [filename, title, flags, hash]
      properties:
        filename:
          type: string
        title:
          type: string
          description: empty when the title could not be read; show the filename.
        state:
          allOf:
            - $ref: "#/components/schemas/state"
          description: absent when state could not be read; the lane carries the effective placement.
        created:
          type: string
        tags:
          type: array
          items:
            type: string
        subsystems:
          type: array
          items:
            type: string
          description: parts of the stack this item impacts; most projects don't use them.
        source:
          type: string
        milestone:
          type: string
        log:
          type: array
          items:
            $ref: "#/components/schemas/logEntry"
        flags:
          type: array
          items:
            $ref: "#/components/schemas/flag"
        hash:
          type: string
        dirty:
          type: boolean
          description: >
            true when the item's file is uncommitted in git — modified,
            staged, or untracked alike; absent when git can't answer (no
            git binary, no repository).

    lane:
      type: object
      required: [state, cards, rankedCount]
      properties:
        state:
          $ref: "#/components/schemas/state"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/card"
        rankedCount:
          type: integer
          description: >
            the first rankedCount cards are the lane's ranked prefix; the
            rest are the computed unranked tail.

    board:
      type: object
      required: [project, lanes, orderVersion]
      properties:
        project:
          type: string
          description: the configured project name.
        lanes:
          type: array
          items:
            $ref: "#/components/schemas/lane"
        orderVersion:
          type: string
          description: order.yaml's hash, or the sentinel "absent". absence is a version.
        dirty:
          type: boolean
          description: >
            true when anything under the roadmap directory is uncommitted
            in git — items, order.yaml, and assets alike; absent when git
            can't answer.

    projectStatus:
      type: object
      required: [name, available]
      properties:
        name:
          type: string
        available:
          type: boolean
        error:
          type: string
          description: the repository-level error for an unavailable root.
        dirty:
          type: boolean
          description: >
            the board's uncommitted verdict, judged per project at index
            time; absent when git can't answer.

    projectIndex:
      type: object
      required: [projects, default]
      properties:
        projects:
          type: array
          items:
            $ref: "#/components/schemas/projectStatus"
        default:
          type: string

    itemLanded:
      type: object
      required: [filename, board]
      properties:
        filename:
          type: string
        board:
          $ref: "#/components/schemas/board"

    conflict:
      type: object
      required: [reason, message]
      properties:
        reason:
          type: string
          enum: [item_conflict, order_conflict, slug_collision]
        message:
          type: string
        tempPath:
          type: string
          description: capture collisions; the preserved .capture- draft.
        sourcePath:
          type: string
          description: retitle and rename collisions; the surviving source file.
        destPath:
          type: string
          description: the colliding destination.

    errorResponse:
      type: object
      required: [message]
      properties:
        message:
          type: string
