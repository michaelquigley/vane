# 2026-07-21 — daemon design session

Design session with Michael on the `desktop-integration` roadmap item, which grew past its original question ("tray + dfw windows against specific roots?") into the multi-project shape: ranger as a tray-resident daemon over every configured root, with a project selector in the UI. The spec landed at `docs/future/daemon.md`.

The settled calls, all made in conversation: explicit hand-edited config at `~/.config/ranger/config.yaml` (no discovery, no registration gestures); path-scoped API (`/api/v1/projects/{name}/…`, names on the wire, roots config-private — flo's source-path-privacy move); the daemon as the normal case with `ranger serve` surviving as the ad-hoc single-project degenerate case of the same server; per-project degradation under the daemon (a bad root flags in the selector, never kills the process) with fail-fast reserved for the daemon's own bootstrap and for ad-hoc serve; tray menu of open-board + quit, browser windows via URL, multiple windows free and uncoordinated. `dfw/tray` only — Michael explicitly chose browser windows over webview windows, keeping the CGO/webview dependency out.

Provisional in the spec, awaiting Michael's reaction: the command name (`ranger daemon`), the URL shape (`/p/{name}`), the projects-index endpoint shape, and the config field names. Scope boundary recorded: this is switching, not aggregation — `cross-repo-aggregation` stays on horizon, and the selector world is additive toward it.

Next: work order grounded in the code, then mercurius over spec + work order together. Worth remembering for the work order: the asset route must gain the project segment (`/roadmap/{name}/…`), the modal's URL transform needs the current project, and the spec change regenerates both sides of the ogen contract.

Spec approved by Michael on read. Work order drafted at `docs/future/daemon-work-order.md`: five stages — (1) config + project set, (2) the breaking contract restructure with server, assets, serve, and SPA mechanics moving together, (3) the selector, (4) the dfw tray daemon, (5) docs close-out. The one UI seam worth naming: `api.ts` becomes a project-bound client factory so call sites don't thread the project name. `mercurius.yaml` calibrated for the arc: review_focus names the two artifacts and the four scrutiny targets (contract completeness, error tiers, seam census calls, stage greenness); settled_decisions carries the re-earned corner-case-defensiveness guard (v1 + the 2026-07-20 race veto) and the spec's recorded deferrals; review_context gains the convention-primacy framing. Awaiting Michael's review of work order + calibration, then mercurius.

## the mercurius arc, converged

Six rounds (session `s_1YliYZO9qRJS`, synopsis at `.mercurius/s_1YliYZO9qRJS/_synopsis.md`): 18 findings, all fixed, none rejected, converging cleanly — 6+1 majors/advisories, then 3+2, 1+1, 1, 2, then round six **ready_to_build** with zero findings. The reviewer's catches were consistently grounded: the live-config-vs-startup-set contradiction, absolute paths leaking through diagnostics (settled as "names address, paths inform"), the fatal-panel selector trap, project-name URL grammar (settled with the convention's own slug rule), serve bypassing that same grammar, the capture-without-preflight wrong-tree write, and the transform-normalization containment hole (settled as a resolved-path prefix check, post-browser-normalization). Michael sharpened one call mid-review: `SpawnWindow` stays unset — no webview, ever — with one lowercase `open board` TrayItem.

Post-close self-audit over both artifacts end-to-end (the drift pass mercurius can't do) caught: the project-index response under-described in the spec (missing `default`, which the `/` redirect consumes); "name is the basename" surviving pre-slug-rule in the config section; `{name}`/`{project}` placeholder drift between the artifacts (harmonized to `{project}` for API and asset routes; `/p/{name}` stays the SPA shape in both); a stale "21 UI tests" count; and the critical-files table missing the modal props change and the tray icon asset. Michael blessed both provisional calls — `ranger daemon` and `/p/{name}` — and the markers are stripped; the spec header now records the converged-through-review provenance. Both artifacts are handoff-ready. Remaining before implementation begins: Michael's implementation-agent selection (Codex default for plan-following; Claude Code if stage 2's contract restructure wants creative latitude), and the working tree still carries the day's un-committed artifacts.

## implementation, stage 1 — config and the project set

Implementation agent (Claude Code) session against the converged work order. Stage 1 landed:

`internal/config`: `Load(path)` dd-binds the YAML (path is a parameter; the daemon passes `~/.config/ranger/config.yaml` in stage 4) and normalizes fail-loud per the work order — empty/absent `projects` refused, `~` expanded via `os.UserHomeDir`, still-relative-after-cleaning roots refused (no resolution base exists), names defaulted through `model.Slug(filepath.Base(root))` with empty-slug and non-slug-override refusals, duplicates refused naming both roots, `default` validated (absent → first), `port` defaulted through a new `config.DefaultPort` constant (4114 — serve's flag can share it when stage 2 rewires).

`internal/server/projects.go`: `Source` is `func() (*config.Config, error)`, consulted fresh on every call — the freshness discipline lives in the source, not in `Projects`. `Resolve(name)` maps name → `workspace.New(root)`; a miss is a typed `UnknownProjectError` (house style: typed like the document layer's errors, so stage 2's handler can `errors.As` it to the 404). `Index()` enumerates with availability judged by a fresh `Load()` per project at call time. No cached anything; `TestSourceConsultedFresh` pins the promise.

Green: full Go suite + vet, 21 UI tests, `make build`. No user-visible change; serve still runs the old wiring. Terminus: clean, zero findings.

## implementation, stage 2 — the contract turns project-scoped

The breaking stage, landed as one green movement:

- `specs/ranger.yml`: every path under `/projects/{project}` with a shared parameter, a `404` (`notFound` → `errorResponse`) on every scoped operation, `GET /projects` → `projectIndex {projects: [{name, available, error?}], default}`. `make generate` regenerated both sides; ogen disambiguates same-schema statuses per operation, so the handlers' 400s became `CreateItemBadRequest`/`RetitleItemBadRequest`/`RenameToSlugBadRequest` and the three ItemLanded ops got per-op `…NotFound` types (plain `*api.ErrorResponse` remains the 404 for the board-returning ops).
- `internal/server`: `Server{projects *Projects}`; a `resolve()` helper maps `UnknownProjectError` → 404 body and lets a config-source failure ride as the request's plain error (heals on next good save). `freshBoard(w, project)` became a function; `board.project` now carries the configured name. `CreateItem` gained the fresh-`Load()` preflight between resolution and `CreateDraft`. `NewError`/`asConflict` untouched per the work order.
- `assets.go`: `ProjectAssets(projects)` — leading segment resolved through the source, then delegation to the untouched `Assets`; unknown project 404, source failure surfaces 500 plainly.
- `cmd/ranger/serve.go`: `config.Synthesize(root, port)` (the shared `normalize` extracted in `internal/config` — one grammar, both callers, errors tagged with an `origin`), `serveBootstrap` (synthesize → resolve → startup `Load()` fail-fast → mux), and `buildMux` — the assembly the daemon will share in stage 4.
- UI: `api.ts` → `makeApi(project)` factory carrying `project` on the object (so modals take one `api` prop), plus unscoped `fetchProjects()`. `App` split: route on `projectFromPath` (`ui/src/project.ts`, no decoding — slugs need none), `DefaultRedirect` consults the index and `location.replace`s to `/p/{default}`, `ProjectBoard` holds the old body with the bound api memoized off the URL project. `markdown.ts` → `makeRoadmapUrl(project)`: resolved-path containment via `new URL` against the `/roadmap/{project}/` base — pathname-prefix check after real URL normalization, inert (`""`) on escape.
- Acceptance tests per the work order: contract census over the parsed spec (`internal/api/census_test.go`); generated-client-through-generated-router round trip (read + mutation + 404) in `TestHTTPRoundTrip`; `TestProjectAssets` cross-project assertions (per-project bytes, unknown 404, four traversal spellings never serving b's bytes under a's name) with the client-side spellings (`../`, `..\`, `%2e%2e`, `.%2e`, host-change) pinned in vitest; `TestDegradationAndHeal` (index flags, board error, capture refusal with byte-for-byte tree comparison, on-disk heal recovering index and board on the same instance); serve's fail-fast tier in `cmd/ranger/serve_test.go`.

Green: full Go suite + vet, 26 UI tests, `make build`, plus a live smoke test (`serve --port 4199`: index, scoped board, 404 message, `/p/ranger`, asset 404).

Terminus on the stage: clean, one advisory — `config.Synthesize` renamed to `config.New` per the constructor convention (applied; suite re-run green). Held for Michael's review; approved, along with the pure-model testing strategy for stage 3 (no component-test stack in `ui/`; state model extracted pure and tested, component a thin map over it — flo's `PracticeSelector` as the shape reference).

## implementation, stage 3 — the selector

- `ui/src/selector.ts`: `selectorOptions(index, current)` — the pure model. Available projects by name; unavailable ones present and disabled with the diagnostic in the label (the spec's own rendering: `anpheq — roadmap directory not found`) and as the tooltip; an unknown current (URL names no configured project) rides as a disabled entry so the header reflects the URL truthfully. `projectPath(name)` landed beside `projectFromPath` in `project.ts` as its dual.
- `App.tsx`: `ProjectSelector` — native `<select>` in the project name's header slot (flo's pattern), `window.location.assign(projectPath(name))` on change: switching is a navigation, never a state mutation. The inversion: `ProjectBoard` fetches the project index beside the board; only the *index* failing keeps the pre-board fatal page (daemon tier — nothing to select); a board failure now renders in a `.board-error` body region under the live header, unknown and unavailable projects alike, so a broken default never strands the operator. `ItemModal` render gated on `board` presence (body-region error means no orderVersion to hand it).
- `style.css`: `.project-selector` as a quiet control (transparent until hover, carries the h1's weight, warn-colored disabled options), `.board-error` padding.
- Tests: `selector.test.ts` — available naming, unavailable flag+diagnostic, the trap case (unavailable default + healthy sibling: current present-disabled-flagged, sibling enabled, `projectPath` navigation), unknown-current phantom entry. 31 UI tests total; full Go suite + vet + `make build` green; live smoke (index + SPA route) good. `document.title` confirmed reading the configured name (follows `board.project`, which stage 2 made the configured name).

Terminus on the stage: clean, zero findings. Held for Michael's review before stage 4; approved.

## implementation, stage 4 — the daemon

- Dependency: `github.com/michaelquigley/dfw v0.1.0`, tray subpackage only (transitives: `fyne.io/systray`, dbus). `CGO_ENABLED=0 go build` confirmed clean — no webview headers anywhere near the binary.
- `cmd/ranger/daemon.go`: `ranger daemon`. `daemonBootstrap(path)` validates the config once — fail-fast naming the path and the fix — then assembles the shared `buildMux` over a file-backed source that `config.Load`s per request; deliberately no per-root load gate. `resolvePort(cfg, flag, changed)` encodes the precedence on cobra's `Changed` (an explicit `--port 4114` still wins). The `Listen` closure binds `127.0.0.1:{port}` and captures the bound listener's address into the board URL — the tray always points where the listener actually bound, immune to later `port:` edits. `tray.DaemonApp`: AppID `com.michaelquigley.ranger`, title `ranger`, the mark PNG, `SpawnWindow` deliberately unset (no webview, ever), one lowercase `open board` TrayItem plus dfw's built-in quit. Config path via `os.UserConfigDir` (= `~/.config/ranger/config.yaml` on linux).
- `cmd/ranger/browser.go`: `browserArgv(goos, url)` — `xdg-open` on linux, `rundll32.exe url.dll,FileProtocolHandler <url>` on windows (entry point in the argv), plain refusal naming the URL elsewhere; `openBrowser` starts the opener and reaps it (a long-lived daemon accumulates no zombies).
- Icon: `cmd/ranger/tray-icon.png`, 32px, rendered from `favicon.svg` via sharp/libvips (masks render correctly; oksvg couldn't), committed and `go:embed`ded. Render verified visually — the binoculars silhouette carries at tray size.
- Tests: bootstrap fail-fast (missing/invalid/empty config, each naming the path), the bootstrap-boundary mirror (broken root constructs, then reports unavailable over the assembled mux — pinning that root health never creeps into bootstrap), fresh-source visibility (a config edit lands on the very next request, no restart), port precedence including the explicit-default-value case, and per-platform browser argv (the windows branch never runs under a green linux build). Live check: `ranger daemon` with no config fail-fasts naming path + fix. The tray loop itself is the by-hand verification — no headless tray.

Green: full Go suite + vet, 31 UI tests, `make build`, `make headless`. Terminus on the stage: clean, zero findings. Held for Michael's review — the tray loop verification (icon render in a real tray, open board, quit) is his by-hand step — before stage 5.

Michael's review nit (cosmetic, left to my judgment): the bootstrap wrap appended "must name at least one project root" to *every* config error, pointing slightly wrong on a duplicate name or bad override. Fixed by splitting the hint on an `os.Stat`: a missing file gets "create … naming at least one project root", everything else gets "fix … and restart" (the raw load error already names its own repair; dd's `FileError` has no `Unwrap`, hence stat over `errors.Is`). Test now pins that an existing-but-broken config never gets the create hint. cmd tests + build green; live fail-fast re-verified. Stage 4 approved (tray verified by hand on Michael's side).

## implementation, stage 5 — close-out

The record catches up:

- `docs/current/daemon.md` (new): the daemon as built — config grammar and fresh-read discipline (port carve-out included), the tray surface and browser helper, the error-by-tier census in full, serve as the degenerate case, and a "deliberately not" section carrying the spec's still-live deferrals (webview, registration gestures, single-instance, per-project tray entries, macOS) as recorded decisions with their revisit triggers.
- `docs/current/ui-and-serve.md`: reshaped — serve as the single-project instance pointing at daemon.md; project-scoped mux (`/api/v1/projects/{project}`, `/roadmap/{project}/`, `/p/{name}` fallback); a "the project in the URL" section covering the selector, the explain-don't-hide rendering, and the body-region inversion; the modal's resolved-path containment; `makeApi` factory in the build story.
- `docs/current/api.md`: the project-scoped contract — index endpoint, per-operation surface with the capture preflight, names-address/paths-inform, the 404 tier joining the error family, and the four acceptance checks in the tests section.
- `docs/current/workspace-and-cli.md`: serve/daemon in the CLI list (one server, two entry commands); CLI gestures never consult the config — in-repo, discovery is the addressing.
- `CHANGELOG.md`: FEATURE (daemon + selector), CHANGE (breaking project-scoped contract restructure, `board.project` semantics shift).
- `README.md`: the daemon is the headline serving story; serve beside it as the ad-hoc path; quick start annotated.
- `docs/future/daemon.md` + `daemon-work-order.md` removed (committed at `792b1eb`; realized-document discipline — deferred content re-synthesized into daemon.md's "deliberately not" first). `cross-repo-aggregation` untouched on horizon.
- **Awaiting Michael's direction**: `desktop-integration` roadmap item is realized — synthesis into docs/current is done; deletion is his call (agents never delete items).

Green: full Go suite + vet, 31 UI tests, `make build`.

Terminus on the stage: clean, one advisory — `ProjectBoard`'s `fatal` state renamed to `boardError` (truthful-naming: it names the recoverable body-region tier now, with `indexFatal` keeping the page-replacing tier; `DefaultRedirect`'s local `fatal` stays — the index tier genuinely is fatal there). Applied; UI suite + build re-run green. Work order realized end to end; awaiting Michael's close-out review and his call on deleting `desktop-integration`.

## the tray icon, de-janked

Michael's hand verification of the tray surfaced the icon reading janky on his dark linux panel. Three causes compounding: brand blue sinking into the dark panel (neighboring tray icons are light symbolic glyphs), the favicon's fine detail — hinge ring, highlight arcs — turning to mud at the panel's ~22px, and the panel rescaling the 32px source. Ran the mark-redraw session's process: a comparison sheet (scratchpad, three candidates × 22/44/64px × dark/light panels) — (a) current blue full drawing, (b) light-monochrome simplified, (c) white mark on a blue rounded badge. Michael picked **b**. The tray icon is now a committed variant: `cmd/ranger/tray-icon.svg` (light `#dedede`, hinge ring and arcs dropped, ring strokes thickened to 2.6, masks carved wider to match) rendered to `tray-icon.png` at 128px so the panel downscale stays smooth. Known caveat, accepted: the tray gets one static PNG, so the icon is tuned to dark panels — candidate (c) is the theme-proof fallback if that ever bites. daemon.md's icon sentence updated. sharp/libvips remains the render path (`npx sharp-cli -i tray-icon.svg -o tray-icon.png resize 128 128`).

## desktop integration

Ported flo's `desktop integrate` / `desktop remove` to ranger as a small one-off (`cmd/ranger/desktop.go`, ranger's plain-`*cobra.Command` style rather than flo's struct wrappers). `Exec` launches `ranger daemon`; `Comment=your roadmap lives in your repo`; `Categories=Utility;Development;`; no `StartupWMClass` (browser owns the windows, same as flo). The one seam surfaced to Michael: the desktop id. A plain `ranger.desktop` would shadow the ranger file manager's system entry on machines carrying it — user XDG dirs win — which is a sharper cost than the accepted binary-name share. Michael picked the reverse-DNS form: `com.michaelquigley.ranger`, matching the tray AppID. Icons are the brand-blue favicon mark (not the panel-tuned light tray variant) rendered via sharp at flo's hicolor sizes (32/48/192/512), committed and embedded in `cmd/ranger/` as `desktop-icon-*.png` — beside `tray-icon.png`, keeping the ui package's headless build-tag split untouched. Live round trip verified against a scratch `XDG_DATA_HOME`. Docs: daemon.md gains "the desktop entry" section, workspace-and-cli.md's CLI list and the README daemon paragraph gain the command, CHANGELOG FEATURE under Unreleased.

Terminus (`46313e160809`): clean, one advisory — the install/remove test claimed removal coverage while deleting with raw `os.Remove` (flo's own test has the same shape). Applied: the test now executes `newDesktopRemoveCmd` twice against a `t.Setenv` data home, pinning both deletion and the already-gone-files-skip. Suite + vet re-green. Held for Michael's review.
